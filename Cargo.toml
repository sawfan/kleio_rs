[package]
name = "kleio"
version = "0.1.0"
edition = "2024"
exclude = [
    "vendor/",
    "*.nt",
    "*.nt.bz2",
]

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
rkyv = "0.8.16"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
