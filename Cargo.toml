[package]
name = "kleio"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
rkyv = "0.8.16"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
bzip2 = "0.5"
